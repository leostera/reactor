(executable
  (name spawn)
  (modules spawn)
  (libraries reactor 
    logs logs.fmt fmt fmt.tty))

(executable
  (name startup)
  (modules startup)
  (libraries reactor ))

(executable
  (name message_passing)
  (modules message_passing)
  (libraries reactor
    logs logs.fmt fmt fmt.tty))
